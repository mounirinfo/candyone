"use client";

import { Typography, styled } from "@mui/material";

const SoftSubtitle = styled(Typography)(({ theme }) => ({
  color: "#fff",
  fontWeight: 800,
  fontSize: 36,
  textShadow: "0 3px 18px rgba(0,0,0,.25)",
}));

export default SoftSubtitle;